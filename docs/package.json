{
    "name": "preact-aria-widgets-demo",
    "version": "0.0.1",
    "type": "module",
    "description": "Demo page for preact-aria-widgets",
    "main": "index.tsx",
    "packageManager": "pnpm@8.6.11",
    "browserslist": "defaults, iOS >= 12",
    "scripts": {
        "watch": "npx esbuild ./index.tsx --bundle --outfile=bundle.js --format=iife --sourcemap --watch",
        "build": "npx esbuild ./index.tsx --bundle --outfile=bundle.js --format=iife --sourcemap --analyze",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "esbuild": "^0.18.18",
        "serve": "^14.2.0"
    },
    "dependencies": {
        "core-js": "^3.32.0"
    }
}